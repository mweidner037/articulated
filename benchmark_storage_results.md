# Current representation comparison

The previous mixed-format report has been replaced by
[the self-contained four-field run comparison](format_comparison.md).

That document shows examples first, then one table containing only this concept's
objects, tuples, columns, typed/binary arrays and packed representations, followed
by the measurement method, limitations and reproduction commands.
