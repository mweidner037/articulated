{
  "private": true,
  "author": "Matthew Weidner",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@istanbuljs/nyc-config-typescript": "^1.0.2",
    "@types/chai": "^5.2.3",
    "@types/mocha": "^10.0.10",
    "chai": "^6.2.2",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "mocha": "^11.7.6",
    "nyc": "^18.0.0",
    "prettier": "^3.9.6",
    "prettier-plugin-organize-imports": "^4.3.0",
    "tsx": "^4.23.1",
    "typedoc": "^0.28.20",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.65.0"
  },
  "scripts": {
    "bench": "pnpm --filter benchmarks start",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "lint:tsc": "pnpm -r run lint:tsc",
    "lint:eslint": "pnpm -r run lint:eslint",
    "lint:prettier": "pnpm -r run lint:prettier",
    "format": "pnpm -r run format",
    "test": "pnpm -r run test",
    "coverage": "pnpm -r run coverage:run",
    "docs": "pnpm -r run docs:run",
    "clean": "pnpm -r run clean"
  },
  "engines": {
    "node": ">=24",
    "pnpm": ">=11"
  }
}
